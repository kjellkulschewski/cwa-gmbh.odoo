<odoo>
  <data>

    <!-- list view -->

    <record id="estate_property_property_types_list" model="ir.ui.view">
      <field name="name">PropertyTypesList</field>
      <field name="model">estate.property.type</field>
      <field name="arch" type="xml">
        <tree>
          <field name="sequence" widget="handle"/>
          <field name="name" string="Property Type"/>
        </tree>
      </field>
    </record>

    <!-- form view -->

    <record id="estate_property_property_types_form" model="ir.ui.view">
      <field name="name">PropertyTypesForm</field>
      <field name="model">estate.property.type</field>
      <field name="arch" type="xml">
        <form>
          <sheet>
            <h1>
                <field name="name"/>
            </h1>
            <notebook>
              <page string="Properties">
                <field name="property_ids">
                  <tree>
                    <field name="name" string="Title"/>
                    <field name="expected_price"/>
                    <field name="state" string="Status"/>
                  </tree>
                </field>
              </page>
            </notebook>
          </sheet>
        </form>
      </field>
    </record>

    <!-- action -->

    <record id="estate_property_property_types_action" model="ir.actions.act_window">
      <field name="name">Property Types</field>
      <field name="res_model">estate.property.type</field>
      <field name="view_mode">tree,form</field>
    </record>

  </data>
</odoo>